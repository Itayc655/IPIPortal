================================================================================
  IPI PORTAL — TO DO LIST
  Last updated: April 21, 2026
================================================================================


────────────────────────────────────────────────────────────────────────────────
COMPLETED ✅
────────────────────────────────────────────────────────────────────────────────

  [✅] SEC-1  AES encryption key moved to ENCRYPTION_KEY env var
  [✅] SEC-2  Admin password hashed with bcrypt (local done, production pending)
  [✅] SEC-3  PowerShell injection: input validated with regex before PS call
  [✅] SEC-4  DB credentials moved to env vars, no hardcoded fallbacks
  [✅] SEC-5  Password reveal PIN checked server-side via /api/verify-pin
  [✅] SEC-6  Admin user list moved to NEXT_PUBLIC_ADMIN_USERS env var
  [✅] SEC-7  Dev bypass controlled by NEXT_PUBLIC_DEV_ADMIN env var
  [✅] SEC-8  editMode URL parameter protected — requires valid JWT cookie
  [✅] SEC-9  All POST /api/sections writes require valid JWT → 401 if invalid
  [✅] REL-7  Holiday settings loaded from server on page mount
  [✅] MAINT-6 Legacy /api/verify-password endpoint deleted


────────────────────────────────────────────────────────────────────────────────
PRODUCTION PENDING  (do these immediately after first production deploy)
────────────────────────────────────────────────────────────────────────────────

  [ ] PROD-1  Hash EditPassword in production DB
              Create scripts/hash-password.mjs, run it once, delete immediately.
              Script content:
                import bcrypt from 'bcryptjs';
                import sql from 'mssql';
                const config = { user:'sa', password:'123456', server:'localhost',
                  database:'ProductHub', options:{instanceName:'SQLEXPRESS',
                  encrypt:false, trustServerCertificate:true} };
                const hash = await bcrypt.hash('123456', 10);
                const pool = await sql.connect(config);
                await pool.request().input('hash', sql.NVarChar, hash)
                  .query("UPDATE GlobalSettings SET SettingValue=@hash WHERE SettingKey='EditPassword'");
                console.log('Done:', hash);
                await pool.close();
              Run: node scripts/hash-password.mjs
              Then: del scripts\hash-password.mjs
              Effort: ~5 minutes

  [ ] PROD-2  Delete dynamic_db.json from production server
              It is legacy, not used, and contains stale data.
              Effort: ~2 minutes


────────────────────────────────────────────────────────────────────────────────
PHASE 3 — RELIABILITY
────────────────────────────────────────────────────────────────────────────────

  [ ] REL-1  Add CSRF protection to POST endpoints
              Next.js 16 has some built-in same-origin protection.
              Add explicit CSRF tokens if portal is accessible outside org network.
              Effort: ~1 hour

  [ ] REL-2  Add rate limiting on /api/auth and /api/user
              Action: npm install @upstash/ratelimit (or simple in-memory counter)
                      Limit /api/auth: 10 attempts per IP per minute
                      Limit /api/user?search=: 30 per IP per minute
              Effort: ~1 hour

  [ ] REL-3  Add Zod input validation on all POST bodies
              Files: app/api/sections/route.ts, app/api/auth/route.ts,
                     app/api/messages/route.ts, app/api/phonebook/route.ts
              Action: npm install zod
                      Define schemas per endpoint, return 400 on failure
              Effort: ~2 hours

  [ ] REL-4  Add size limit and cleanup to user cache
              File: lib/auth.ts, userCache Map
              Action: Add max-entries check (~500 users), evict oldest on overflow
                      Or: npm install lru-cache
              Effort: ~30 minutes


────────────────────────────────────────────────────────────────────────────────
PHASE 4 — FEATURES
────────────────────────────────────────────────────────────────────────────────

  [ ] FEAT-1  Replace PowerShell AD queries with ldapjs
              Files: lib/auth.ts, app/api/user/route.ts
              Note: ldapjs already in package.json
              Benefits: faster, no injection risk, cross-platform
              Effort: ~4 hours

  [ ] FEAT-3  Phonebook schema editor in admin UI
              File: app/ClientPage.tsx
              Action: Modal to add/remove/rename phonebook columns,
                      similar to section schema builder
              Effort: ~2 hours

  [ ] FEAT-4  Audit logging for admin actions
              Action: Create AuditLog SQL table (timestamp, username, action, target)
                      Write to it on all create/update/delete in /api/sections
              Effort: ~2 hours

  [ ] FEAT-5  Dedicated phonebook search field
              File: app/ClientPage.tsx
              Action: Add search input that filters phonebook independently
                      from the main section cards search
              Effort: ~1 hour

  [ ] FEAT-7  Replace browser confirm() dialogs with custom modals
              File: app/ClientPage.tsx
              Action: Custom confirmation modal showing item name,
                      replaces all confirm() calls
              Effort: ~1.5 hours

  [ ] FEAT-8  Color picker for sections
              File: app/ClientPage.tsx (section builder modal)
              Action: Let admin pick color instead of auto-assigning round-robin
              Effort: ~1 hour

  [ ] FEAT-9  Card drag-and-drop ordering
              Files: app/ClientPage.tsx + app/api/sections/route.ts
              Note: dnd-kit already used for phonebook and schema fields
              Action: Extend to cards, add sortOrder column to Items table
              Effort: ~3 hours


────────────────────────────────────────────────────────────────────────────────
PHASE 5 — CLEANUP & MAINTENANCE
────────────────────────────────────────────────────────────────────────────────

  [ ] MAINT-1  Split ClientPage.tsx into smaller components
               Current size: ~1800 lines
               Suggested split:
                 components/portal/SectionGrid.tsx
                 components/portal/Phonebook.tsx
                 components/portal/SystemMessagesTicker.tsx
                 components/modals/ViewItemModal.tsx
                 components/modals/AddItemModal.tsx
                 components/modals/SectionBuilderModal.tsx
                 components/modals/LoginModal.tsx
                 components/modals/SecurityModal.tsx
               Effort: ~4 hours (mechanical, low risk)

  [ ] MAINT-2  Move constants to lib/constants.ts
               Action: AUTO_COLORS array, DEFAULT_PHONEBOOK_SCHEMA
               Effort: ~20 minutes

  [ ] MAINT-3  Add TypeScript interfaces for core data shapes
               Action: Create types/portal.ts with:
                 Section, Item, PhonebookRow, SystemMessage,
                 FieldSchema, UserProfile, VisibilityConfig
               Effort: ~1.5 hours

  [ ] MAINT-4  Add error boundaries around main UI sections
               Action: Wrap SectionGrid, Phonebook, SystemMessages
               Effort: ~1 hour

  [ ] MAINT-5  Add health check endpoint
               Action: Create app/api/health/route.ts
                       Returns { status: 'ok', db: 'connected' } or 500
               Effort: ~20 minutes

  [ ] MAINT-7  Write README.md with setup instructions
               Cover: .env setup, SQL table creation, dev server,
                      PM2+IIS deploy, adding admin users
               Effort: ~1 hour


────────────────────────────────────────────────────────────────────────────────
QUICK WINS  (under 30 minutes each)
────────────────────────────────────────────────────────────────────────────────

  [ ] PROD-1  Hash EditPassword on production DB          (~5 min)
  [ ] PROD-2  Delete dynamic_db.json from production      (~2 min)
  [ ] MAINT-5 Add /api/health endpoint                    (~20 min)
  [ ] MAINT-2 Move constants to lib/constants.ts          (~20 min)
  [ ] REL-4   User cache size limit                       (~30 min)


────────────────────────────────────────────────────────────────────────────────
SUGGESTED ORDER FOR NEXT SESSIONS
────────────────────────────────────────────────────────────────────────────────

  Next session:    PROD-1 + PROD-2 (production cleanup after deploy)
                   Then REL-1, REL-2, REL-3 (Phase 3 reliability)
  Following:       FEAT-1 (ldapjs — removes PowerShell dependency)
  Rainy day:       MAINT-1 (split ClientPage.tsx)


================================================================================
SUMMARY
  Completed:  11 items (Phase 1 + Phase 2 + MAINT-6)
  Remaining:  2 production tasks + 4 reliability + 7 features + 6 cleanup = 19
  Total:      30 items
================================================================================